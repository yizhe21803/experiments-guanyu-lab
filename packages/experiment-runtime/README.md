# GUANYU LAB Experiment Runtime

Cross-experiment runtime boundary only: auto-demo lifecycle, manual takeover, palette controller, resize/bootstrap and version metadata. Runtime infrastructure must never alter experiment-specific shader output or physics.
